<UserControl x:Class="Contracts.UserControls.LabelProgressBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="32" d:DesignWidth="350">
    <Grid Height="28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{Binding ProgressString, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="Center"
                   FontSize="13" FontWeight="SemiBold"
                   Foreground="#444"/>

        <Border Grid.Column="1" CornerRadius="4" Background="#E8E8E8" Margin="4,2">
            <Border CornerRadius="4" ClipToBounds="True">
                <ProgressBar x:Name="progressBar"
                             Minimum="0" Maximum="100"
                             Value="{Binding ProgressValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Height="20"
                             BorderThickness="0"
                             Foreground="#6C63FF"
                             Background="Transparent"/>
            </Border>
        </Border>

        <TextBlock Grid.Column="2"
                   Text="{Binding ElementName=progressBar, Path=Value, StringFormat={}{0:0}}"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Foreground="#666"
                   FontSize="12" FontWeight="SemiBold"/>
    </Grid>
</UserControl>
